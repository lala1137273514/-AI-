# 2026-06-07（周日）加班任务清单

> 加班时段 01:46–22:06（按 git commit 时间戳，全天 27 个 clawd commit + 4 个 cortex commit）。三条线：clawd glassbox Phase2 语音遥控、cortex 方案三选一、Loona 周报/OKR 与组件规范收尾。

## 1. clawd glassbox 语音遥控 Phase2（27 commit · 01:46–22:06）

**核心链路打通（凌晨 01:46 起）**
- 截图捕获模块（`80ba8ec`）
- 编排器接百炼 qwen 轻模型（`4cb3b72`）
- 派活：spawn claude / codex 子进程执行（`fd78393`）
- 接进主 app（`1b0306e`）
- 收尾 recap + 二次确认（`659f17f`）
- whisper 识别修复（`86687c2`）
- Ctrl+Space 热键 + VAD 语音活动检测（`064c575`）

**交互与稳健性（14:02–14:33）**
- 派活改走 stdin 注入（`56cdf6f`）
- Spotlight 式输入条（`2b23a66`）
- 沙箱修复（`0addf0b`）+ 截图链路加固（`237ccce`）
- 交接 README（`7f3dc53`）

**架构外置 + 活泼度模块（18:00–18:08）**
- 编排器 prompt 外置（`8dea61c`）+ 语义化阶段（`c281b86`）
- 活泼度子系统：phase-ui / resolver / speech / demo / wake-word 五模块（`42752a6`）
- 设置项（`8f490fb`）+ 接 glass-box 条 + 桌宠 + 热键（`f08c027`）
- 确认策略路由 Phase2b（`ba8de45`）
- 全量 3647 测试通过

**桌宠表现力 + 唤醒词 + demo 自循环（18:51–22:06，晚上新增）**
- 实时步骤挂到桌宠思考气泡，不再占输入条（`c4dfec6`）
- demo 自循环：展台自动跑 + 自校验（`aab9dd4`）
- 主动监工 + 桌宠跟随气泡 + 旋钮解析（settings>env>default）（`ec5bbde`）
- **「hey, cc」唤醒词落地**（能量门控 + 本地 whisper，默认关）（`1ffa289`）
- app 内语音遥控旋钮设置面板（direction 4）（`39cb76e`）
- 阶段→桌宠状态映射更细（capturing→sweeping、dispatching→carrying）（`bd53083`）+ 成功收尾撒花动画（`2ce224c`）
- 气泡/特效对齐到桌宠可见部分（贴边打磨）（`3b774a4`）+ 新桌宠 Bloop（手绘 CSS SVG 状态）（`6c1f96a`）

## 2. cortex 方案三选一（4 commit · 17:47–19:46）

- 后端 plan_options 路由：方案三选一（`a52dcf8d`）
- prompt 插入 + 澄清流程改 3 轮（`82301ddc`）
- 汇总卡走 confirmed/assumed/title 修复（`062fa0db`）
- 旅游流程拆两轮，plan_options 独占无工具一轮触发 compose_options（`37fc25dd`）

## 3. Loona 周报 / OKR / 组件规范收尾（无 commit，文档与对齐）

- Jarvis × cortex 联调：字幕一段段蹦、TTS 延迟两个 bug 排查修复，跑测试矩阵，补架构/验收文档。
- loona-workbench 组件规范：9 类卡片 × 字段表 × 22 张组件图。
- W8 周报 + OKR 进展整理并回填（含落文档前的去 AI 味 + 内部预审双质检）。

## 产出小结

- glassbox Phase2 把「语音唤醒 → 截图理解 → 轻模型编排 → 派活给 claude/codex → 收尾确认」整条遥控链路跑通；晚上又把表现力做足——桌宠思考气泡、demo 自循环、撒花动画、新桌宠 Bloop，并把「hey, cc」唤醒词落地（默认关），3647 测试全绿，demo 已能上展台自动跑。
- cortex 方案层补上「三选一」后端路由，澄清收敛到 3 轮，旅游流程拆成两轮触发 compose_options。
- 周报/OKR 与组件规范收口，工程联调把字幕和 TTS 两个体验 bug 解掉。

> 关联 KR：glassbox 语音遥控 → O2-KR1（AI 赋能场景）；cortex 方案三选一 → O2-KR3（场景跑通落地）；组件规范 → O4-KR4（验收物料）；周报/OKR 复盘 → O1-KR3（协作习惯）。
