name: Secret check

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run secret check
        shell: pwsh
        run: ./scripts/check-secrets.ps1
